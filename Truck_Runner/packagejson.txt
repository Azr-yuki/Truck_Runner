{
  "name": "truck-runner-proxy", // Project name (used by npm)
  "version": "1.0.0",           // Version for tracking updates (version control)
  "description": "CORS proxy for Truck Runner banana API", // Describes purpose
  "main": "server.js",           // Entry file for Node.js
  "type": "module",              // Uses ES modules (import/export) instead of CommonJS
  "scripts": {
    "start": "node server.js"    // Script to run the server: npm start
  },
  "dependencies": {              // Dependencies installed via npm
    "express": "^4.18.2",        // Web server framework (interoperability: lets your frontend fetch from localhost)
    "node-fetch": "^3.3.2",      // Fetch API for Node.js (used to call Banana API)
    "cors": "^2.8.5"             // Handles CORS (cross-origin requests)
  }
}
